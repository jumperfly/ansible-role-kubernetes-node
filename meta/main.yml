dependencies:
  - src: jumperfly.ssl_cert
    version: v0.2.0
    ssl_cert_ca_delegate_name: "{{ kube_cert_ca_delegate_name }}"
    ssl_cert_type: client
    ssl_cert_file_base_dir: /srv/kubernetes
    ssl_cert_name: "{{ kube_cert_name }}"
    ssl_cert_subject: "{{ kube_cert_subject }}"
  - src: jumperfly.kubelet
    version: v0.1.0
    kubelet_args: "{{ kube_node_kubelet_args }}"

galaxy_info:
  role_name: kubernetes_node
  author: jumperfly
  description: ansible-role-kubernetes-node
  license: Apache
  min_ansible_version: "2.0"
  platforms:
    - name: EL
      versions:
        - "7"
